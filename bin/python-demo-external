#!/usr/bin/env python3

import subprocess

def main():
    # these require coreutils
    subprocess.call(['ls'])
    subprocess.call(['echo hello'], shell=True)

if __name__ == '__main__':
    main()
